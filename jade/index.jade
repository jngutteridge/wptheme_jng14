- get_header()
- if (have_posts()) : the_post()
  main
    article.main.page
      header.page
        .container
          h1!= get_the_title()
      section.page.content
        .container
          - the_content()
      footer.end.page
        .container
          p You shouldn't be reading this, naughty bugger.
- else : go_do_error();
- endif
- get_footer()
